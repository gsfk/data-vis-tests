import React from "react"
import {
  Slice,
  VictoryContainer,
  VictoryLabel,
  VictoryLegend,
  VictoryPie,
} from 'victory';

const PieTable = ({data}) => {
  return <h1>{data}</h1>;
}

export default PieTable;